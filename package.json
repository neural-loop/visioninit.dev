{
  "name": "visioninit-website",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "hugo server --buildFuture",
    "build": "npm run clean && npm run generate-favicons && npm run og-images && hugo --gc --minify --templateMetrics --templateMetricsHints --buildExpired --buildFuture --forceSyncStatic",
    "test": "hugo server --disableFastRender --navigateToChanged --templateMetrics --templateMetricsHints --buildDrafts --buildExpired --buildFuture --watch --forceSyncStatic -e production --minify",
    "clean": "rm -rf public/ resources/ tmp/ static/favicon* static/apple-touch-icon.png static/logo.png content/**/og.png content/**/temp-og.html",
    "lint:scss": "stylelint \"assets/scss/**/*.scss\"",
    "lint:scss:fix": "stylelint \"assets/scss/**/*.scss\" --fix",
    "project-setup": "node ./scripts/projectSetup.js",
    "theme-setup": "node ./scripts/themeSetup.js",
    "og-images": "node scripts/generateOGImages.js",
    "generate-favicons": "node scripts/generateFavicons.js"
  },
  "dependencies": {
    "mime-types": "^3.0.1"
  },
  "devDependencies": {
    "postcss-scss": "^4.0.9",
    "stylelint": "^16.19.1",
    "stylelint-config-standard-scss": "^14.0.0"
  }
}
